# wiki-tech-revenue-pipeline
An end-to-end data pipeline that scrapes historical revenue data for the world's largest technology companies from Wikipedia, loads it into a SQL Server data warehouse using a Bronze → Silver → Gold medallion architecture, and runs analytical queries on top of the cleaned data.
